export const setAccessToken = (value: string) => {
  try {
  } catch (error) {}
};

export const setRefreshingToken = (value: string, userId: number) => {};

export const getAccessToken = (value: string) => {};
